Pre-exam review for: $ARGUMENTS

1. Run the `build-cheat-sheet` workflow for the scope given above (a course, unit, or lecture
   range).
2. Then run a `quiz-me` session (5-8 questions) covering the same scope, using the cheat sheet's
   concept list to make sure every major concept gets at least one question.
3. At the end, summarize which concepts showed weak spots so the next review session can focus
   there.
